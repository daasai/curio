
## 代码 Review（全项目）

完成 Curio 全项目代码审查，覆盖单文件原型(app.js 1372行) + monorepo(apps/web React19 + apps/api Bun/Hono + packages/db Drizzle + 12 scripts)。

**关键发现：**
- P0 安全：apps/api/src/index.ts 旧路由完全无鉴权(明文 token_${userId})，与新 HMAC cookie 鉴权并存上线；auth.ts JWT_SECRET 缺省值；CORS origin:*；前端 dangerouslySetInnerHTML 渲染后端 storyText 有 XSS 风险。
- P1 数据可信度：normalizePhone 正则双反斜杠失效(测试用纯净号掩盖)；scheduler 用 UTC vs learning 用 Asia/Shanghai 时区不一致致 streak 错位；accuracy 无数据返回 80 假数据；Onboarding 偏好/诊断数据未上报(setupUserPreferencesApi 从未调用)，P1 基线丢失。
- 架构亮点：数据模型 10 表含 SM-2 字段、事件溯源(learningSessions+learningEvents)、部署 deploy.sh 规范。
- 文档偏差：CONTEXT.md:71 称「apiClient 未调用」已过时，实测 17 函数中 10 个已用，核心学习闭环已打通，真正缺口是 Onboarding 持久化 + 8 个 v1 死函数。

详见对话内 review 报告。

## 编写升级 Spec v0.5.0

基于 2026-08-08 review 发现，在 v0.4.1 基础上编写 `docs/spec/spec-p1-completion-and-p2-mvp-v0.5.0.md`。

核心定位：增量修订而非推翻。关键判断——review 大部分问题是代码偏离了 v0.4.1 已有规则(越权违反不变量#2、假数据违反#9、时区违反#10)，真正 spec 缺口在：normalizePhone 算法、XSS 净化、Onboarding 写入接口、密钥管理、SM-2 状态机转移图、JSON 解析容错、前端工程规范、部署安全。

新增章节：§16 安全与密钥管理、§17 前端工程规范、§18 部署安全配置、§19 文档同步。增补不变量#13-18。插入 Phase 0.5(旧路由下线+安全加固，试点硬门禁)。补测试用例 16-22。增补 DoD 验收项。

待办：落地后需新增 ADR-0007 记录安全加固决策；同步更新 CONTEXT.md 前后端打通条目。
